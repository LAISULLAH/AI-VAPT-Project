services:
  - type: web
    name: ai-vapt-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y nmap
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
